Decoy vault: three signals gave it away from a decoy session.

Inheritance is now per vault:
• Setting it up from the decoy no longer says "main vault only".
• The inactivity counter no longer points at the decoy.
• Deleting the decoy also erases its inheritance, leaving file sizes unchanged.

After setting up a decoy, the app finally returns to the lock screen.

The store listing wrongly claimed panic mode destroys the Keystore key.
